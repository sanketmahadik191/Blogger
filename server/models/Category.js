// const mongoose= require('mongoose');

// const categorySchema = new mongoose.Schema({
//     name: {
//       type: String,
//       required: [true, 'Category name is required'],
//       unique: true,
//       trim: true,
//     },
//   });
  
//   const Category = mongoose.model('Category', categorySchema);
//   module.exports = Category;


//to make diff category schema is best approch but for big data beacuse to search by category we have to access every blog but with seprate schema searching will optimized
  